%This is a program to check whether an input string is a palindrome or not
%"palindromes" are words which reads the same- forwards or backwards. example- madam.
str = input('string for palindrome','s');
if str == flip(str);
    s = 1;
else
    s = 0;
end
if s==1
    disp('it is a palindrome')
else
    disp('it is not a palindrome')
end

    
   